metadata.format.version = "1.1"

[versions]

minestom = "commit-info-SNAPSHOT"

# Gradle plugins
blossom = "1.3.0"
shadowJar = "7.0.0"

[libraries]

minestom = { group = "com.github.off-by-0point5", name = "Minestom", version.ref = "minestom" }

[bundles]

[plugins]

blossom = { id = "net.kyori.blossom", version.ref = "blossom" }
shadowJar = { id = "com.github.johnrengelman.shadow", version.ref = "shadowJar" }
